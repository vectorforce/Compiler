package ast.parser;

public enum TokenType {       // Enumeration for all types of tokens
    NUMBER, WORD, TEXT,

    PRINT, IF, ELSE,

    PLUS, MINUS, STAR, SLASH, EQ, LT, GT,

    LPAREN, RPAREN,

    EOF
}
